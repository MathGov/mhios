# MHIOS wireframe

Open `index.html` in a browser. It is a static interaction mock-up using synthetic data, including an Emergency Rights Comparison surface. It does not calculate or authorize a real decision.


## Minimal vertical slice

Open `minimal_vertical_slice.html` for the five-screen, 12-object first-build profile. The existing `index.html` remains the later full-product target.
